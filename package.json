{
  "name": "mixtape",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build && node server.mjs",
    "build": "node scripts/build.mjs && npm run check",
    "download:tracks": "node scripts/download-tracks.mjs",
    "check": "node --check src/app.js && node --check scripts/build.mjs && node --check dist/app.js && node --check dist/tracks.js && node --check server.mjs",
    "test": "node --test tests/catalog.test.mjs"
  }
}
